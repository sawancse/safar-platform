fix below as well:
-----------
1). Handpicked stays across India  
add hyderbad also
2). when i click on PG it's going to search page instead of display pg info 
and when i search pg in search box it's not coming
3).during lisiing creation i see only per night pricing can we include th eoption to include night, month and hour as well
3). for admin page while verifying listing host not able to see which host has raised the request for listing a i think host details should be included.
4). also appy for commercial

========
1). default choose your room : classic deleux suite how it's comfigured. seems it's hardecoded and auto filled- we need a control on this.
======
1). lets say host selected wrong location for listing but not able to edit location after publishing suggest fix for this
=================
3). introduce a chek on admin if host has not verified identity for admin review  host should not be able to veirfy
display info for host identoty as well for admin similar to airbnb
==========
4). during identity verification by host or admin we have should mandatory the document upload of both aadhar , identity card any  fixed

============
5). Hosted by Gunja kumari
🛡
Trusted
Joined January 1970
host joining date is not display correctly  - fixed
=================
6). how to hold , pause or blacklist a host by admin
===========
8). how host or admin can delete a particular photo uploaded by host if photo is not correct or reviewd properly