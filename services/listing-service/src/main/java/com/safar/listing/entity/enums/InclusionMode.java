package com.safar.listing.entity.enums;

public enum InclusionMode {
    INCLUDED,       // Bundled in room rate
    PAID_ADDON,     // Guest pays extra
    COMPLIMENTARY   // Free extra (not in rate, but no charge)
}
