server:
  port: 8080
  netty:
    connection-timeout: 10000

spring:
  application:
    name: api-gateway

  codec:
    max-in-memory-size: 20MB

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      lettuce:
        pool:
          max-active: 8
          max-idle: 4
          min-idle: 1
          max-wait: 2000ms

  cloud:
    gateway:
      httpclient:
        connect-timeout: 5000
        response-timeout: 15s
      # CORS handled by CorsConfig.java bean (highest precedence)

      routes:
        # ── OTP send: rate-limited to 3 req/hour per IP ──────────────────────
        - id: auth-otp-send
          uri: http://${AUTH_SERVICE_HOST:localhost}:8888
          predicates:
            - Path=/api/v1/auth/otp/send
          filters:
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 3
                redis-rate-limiter.burstCapacity: 5
                redis-rate-limiter.requestedTokens: 1
                key-resolver: "#{@ipKeyResolver}"

        # ── All other auth routes (verify, refresh, logout) ──────────────────
        - id: auth-service
          uri: http://${AUTH_SERVICE_HOST:localhost}:8888
          predicates:
            - Path=/api/v1/auth/**

        # ── User profiles & subscriptions ────────────────────────────────────
        - id: user-service
          uri: http://${USER_SERVICE_HOST:localhost}:8092
          predicates:
            - Path=/api/v1/users/**

        # ── Listings (GET public, mutations need JWT) ─────────────────────────
        - id: listing-service
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/listings/**

        # ── Location suggestions (public autocomplete) ────────────────────────
        - id: locations
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/locations/**

        # ── Search (all GET endpoints are public) ─────────────────────────────
        - id: search-service
          uri: http://${SEARCH_SERVICE_HOST:localhost}:8084
          predicates:
            - Path=/api/v1/search/**

        # ── Bookings ──────────────────────────────────────────────────────────
        - id: booking-service
          uri: http://${BOOKING_SERVICE_HOST:localhost}:8095
          predicates:
            - Path=/api/v1/bookings/**

        # ── Payments — webhook first (no JWT), then authenticated routes ──────
        - id: payment-service-webhook
          uri: http://${PAYMENT_SERVICE_HOST:localhost}:8086
          predicates:
            - Path=/api/v1/payments/webhook/**

        - id: payment-service
          uri: http://${PAYMENT_SERVICE_HOST:localhost}:8086
          predicates:
            - Path=/api/v1/payments/**

        # ── Donations (Aashray) ───────────────────────────────────────────────
        - id: donation-service
          uri: http://${PAYMENT_SERVICE_HOST:localhost}:8086
          predicates:
            - Path=/api/v1/donations/**

        # ── Media uploads ─────────────────────────────────────────────────────
        - id: media-service
          uri: http://${MEDIA_SERVICE_HOST:localhost}:8088
          predicates:
            - Path=/api/v1/media/**

        # ── Reviews ───────────────────────────────────────────────────────────
        - id: review-service
          uri: http://${REVIEW_SERVICE_HOST:localhost}:8087
          predicates:
            - Path=/api/v1/reviews/**

        # ── Messaging ────────────────────────────────────────────────────────
        - id: messaging-service
          uri: http://${MESSAGING_SERVICE_HOST:localhost}:8091
          predicates:
            - Path=/api/v1/messages/**

        # ── Guest subscriptions (S18) ───────────────────────────────────────
        - id: guest-service
          uri: http://${USER_SERVICE_HOST:localhost}:8092
          predicates:
            - Path=/api/v1/guests/**

        # ── Video reviews (S19) ─────────────────────────────────────────────
        - id: video-review-service
          uri: http://${BOOKING_SERVICE_HOST:localhost}:8095
          predicates:
            - Path=/api/v1/video-reviews/**

        # ── Wallet (S20) ────────────────────────────────────────────────────
        - id: wallet-service
          uri: http://${USER_SERVICE_HOST:localhost}:8092
          predicates:
            - Path=/api/v1/wallet/**

        # ── Property miles (S23) ────────────────────────────────────────────
        - id: miles-service
          uri: http://${BOOKING_SERVICE_HOST:localhost}:8095
          predicates:
            - Path=/api/v1/miles/**

        # ── Listing groups (S26) ────────────────────────────────────────────
        - id: listing-groups-service
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/listing-groups/**

        # ── Vacant property network (S28) ───────────────────────────────────
        - id: vpn-service
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/vpn/**

        # ── Bucket list & discovery feed (S32) ──────────────────────────────
        - id: bucket-list-service
          uri: http://${USER_SERVICE_HOST:localhost}:8092
          predicates:
            - Path=/api/v1/bucket-list/**

        - id: discovery-service
          uri: http://${USER_SERVICE_HOST:localhost}:8092
          predicates:
            - Path=/api/v1/discovery/**

        # ── Managed stays (S33) ─────────────────────────────────────────────
        - id: managed-stay-service
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/managed-stay/**

        # ── Guaranteed income (S34) ─────────────────────────────────────────
        - id: guaranteed-income-service
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/guaranteed-income/**

        # ── Co-hosting (S35) ────────────────────────────────────────────────
        - id: cohost-service
          uri: http://${USER_SERVICE_HOST:localhost}:8092
          predicates:
            - Path=/api/v1/cohost/**

        # ── Cleaning network (S36) ──────────────────────────────────────────
        - id: cleaning-service
          uri: http://${BOOKING_SERVICE_HOST:localhost}:8095
          predicates:
            - Path=/api/v1/cleaning/**

        # ── Host business suite (S37) ───────────────────────────────────────
        - id: host-business-service
          uri: http://${PAYMENT_SERVICE_HOST:localhost}:8086
          predicates:
            - Path=/api/v1/host/**

        # ── App marketplace (S38) ───────────────────────────────────────────
        - id: marketplace-service
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/marketplace/**

        # ── B2B data intelligence (S39) ─────────────────────────────────────
        - id: b2b-service
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/b2b/v1/**

        # ── Live anywhere (S41) ─────────────────────────────────────────────
        - id: live-anywhere-service
          uri: http://${BOOKING_SERVICE_HOST:localhost}:8095
          predicates:
            - Path=/api/v1/live-anywhere/**

        # ── NomadPrime (S42) ────────────────────────────────────────────────
        - id: nomad-prime-service
          uri: http://${USER_SERVICE_HOST:localhost}:8092
          predicates:
            - Path=/api/v1/nomad-prime/**

        # ── Experiences (S43) ───────────────────────────────────────────────
        - id: experiences-service
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/experiences/**

        - id: experience-bookings-service
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/experience-bookings/**

        # ── Medical tourism (S44) ───────────────────────────────────────────
        - id: medical-stay-service
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/medical-stay/**

        # ── Aashray (refugee housing — cases in listing-service) ────────────
        - id: aashray-service
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/aashray/**

        # ── Channel Manager (Channex.io integration) ──────────────────────
        - id: channel-manager-service
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/channel-manager/**

        # ── Channex webhook (no auth required) ────────────────────────────
        - id: channex-webhook
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/webhooks/channex

        # ── Seeker profiles (reverse search) ──────────────────────────────
        - id: seeker-service
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/seekers/**

        # ── Locality polygons ─────────────────────────────────────────────
        - id: locality-service
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/localities/**

        # ── PG Tenancies ──────────────────────────────────────────────────
        - id: pg-tenancy-service
          uri: http://${BOOKING_SERVICE_HOST:localhost}:8095
          predicates:
            - Path=/api/v1/pg-tenancies/**

        # ── Notifications (in-app) ──────────────────────────────────────────
        - id: notification-service
          uri: http://${NOTIFICATION_SERVICE_HOST:localhost}:8089
          predicates:
            - Path=/api/v1/notifications/**

        # ── Nomad network (S45) ─────────────────────────────────────────────
        - id: nomad-network-service
          uri: http://${USER_SERVICE_HOST:localhost}:8092
          predicates:
            - Path=/api/v1/nomad/**

        # ── Discovery categories (public, India vibe-based browse) ─────────
        - id: discover-service
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/discover/**

        # ── AI service (Python FastAPI — scout & listing generator) ──────────
        - id: ai-service
          uri: http://${AI_SERVICE_HOST:localhost}:8090
          predicates:
            - Path=/api/v1/ai/**

        # ── Admin: user/host management (user-service) ────────────────────────
        - id: admin-user-service
          uri: http://${USER_SERVICE_HOST:localhost}:8092
          predicates:
            - Path=/api/v1/admin/hosts,/api/v1/admin/hosts/**,/api/v1/admin/analytics/**,/api/v1/admin/cohost/**

        # ── Admin: cleaning (booking-service) ────────────────────────────────
        - id: admin-cleaning-service
          uri: http://${BOOKING_SERVICE_HOST:localhost}:8095
          predicates:
            - Path=/api/v1/admin/cleaning/**

        # ── Admin: KYC management (user-service) ──────────────────────────────
        - id: admin-kyc
          uri: http://${USER_SERVICE_HOST:localhost}:8092
          predicates:
            - Path=/api/v1/admin/kyc/**

        # ── Admin: Payouts & Settlements (payment-service) ───────────────────
        - id: admin-payments
          uri: http://${PAYMENT_SERVICE_HOST:localhost}:8086
          predicates:
            - Path=/api/v1/admin/payouts/**,/api/v1/admin/settlements/**,/api/v1/admin/reconciliation/**,/api/v1/admin/commission/**

        # ── Admin: bookings & guests (booking-service) ───────────────────────
        - id: admin-booking-service
          uri: http://${BOOKING_SERVICE_HOST:localhost}:8095
          predicates:
            - Path=/api/v1/admin/bookings/**,/api/v1/admin/guests/**

        # ── Referral System (user-service) ─────────────────────────────────────
        - id: referrals
          uri: http://${USER_SERVICE_HOST:localhost}:8092
          predicates:
            - Path=/api/v1/referrals/**

        # ── Loyalty Tiers (user-service) ──────────────────────────────────────
        - id: loyalty
          uri: http://${USER_SERVICE_HOST:localhost}:8092
          predicates:
            - Path=/api/v1/loyalty/**

        # ── Safar Cooks (chef-service) ──────────────────────────────────────
        - id: chef-profiles
          uri: http://${CHEF_SERVICE_HOST:localhost}:8093
          predicates:
            - Path=/api/v1/chefs/**

        - id: chef-bookings
          uri: http://${CHEF_SERVICE_HOST:localhost}:8093
          predicates:
            - Path=/api/v1/chef-bookings/**

        - id: chef-subscriptions
          uri: http://${CHEF_SERVICE_HOST:localhost}:8093
          predicates:
            - Path=/api/v1/chef-subscriptions/**

        - id: chef-events
          uri: http://${CHEF_SERVICE_HOST:localhost}:8093
          predicates:
            - Path=/api/v1/chef-events/**

        # ── Sale Properties (buy/sell marketplace) ────────────────────────────
        - id: sale-properties
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/sale-properties/**

        - id: property-inquiries
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/inquiries/**

        - id: site-visits
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/site-visits/**

        - id: buyer-profile
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/buyer-profile/**

        - id: locality-trends
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/locality-trends/**

        # ── Sale property search (search-service) ───────────────────────────
        - id: sale-property-search
          uri: http://${SEARCH_SERVICE_HOST:localhost}:8084
          predicates:
            - Path=/api/v1/search/sale-properties/**

        # ── Builder Projects (listing-service) ──────────────────────────────
        - id: builder-projects
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/builder-projects/**

        # ── Builder project search (search-service) ─────────────────────────
        - id: builder-project-search
          uri: http://${SEARCH_SERVICE_HOST:localhost}:8084
          predicates:
            - Path=/api/v1/search/builder-projects/**

        # ── Admin: listing verification & catch-all (listing-service) ────────
        - id: admin-listing-service
          uri: http://${LISTING_SERVICE_HOST:localhost}:8083
          predicates:
            - Path=/api/v1/admin/**

jwt:
  secret: ${JWT_SECRET:l0UNaBuoYCf7Y/G8G1bihPxkUI7NZuPaZdzTbbEYqzM=}

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health,info,metrics,gateway

resilience4j:
  circuitbreaker:
    configs:
      default:
        sliding-window-size: 10
        minimum-number-of-calls: 5
        failure-rate-threshold: 50
        wait-duration-in-open-state: 30s
        permitted-number-of-calls-in-half-open-state: 3
        automatic-transition-from-open-to-half-open-enabled: true
  timelimiter:
    configs:
      default:
        timeout-duration: 15s
        cancel-running-future: true

logging:
  level:
    com.safar.gateway: INFO
    org.springframework.cloud.gateway: WARN
