# Safar Platform — Environment Variables
# Copy this file to .env and fill in real values. NEVER commit .env to git.

# ── Database ──────────────────────────────────────────────────────────────────
DB_USER=safar
DB_PASS=safar_dev
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/safar

# ── Redis ─────────────────────────────────────────────────────────────────────
REDIS_HOST=localhost
REDIS_PORT=6379

# ── JWT (generate a secure base64-encoded 256-bit secret) ────────────────────
JWT_SECRET=l0UNaBuoYCf7Y/G8G1bihPxkUI7NZuPaZdzTbbEYqzM=

# ── AES-256 Encryption (for PII: Aadhaar, PAN, bank account) ────────────────
# Generate with: openssl rand -base64 32
ENCRYPTION_AES_KEY=t+YZDpLpyNyfaZcSbH9Ak2CMYbJjBKzEdJYZR130CDI=
JWT_EXPIRY_MINUTES=15
JWT_REFRESH_EXPIRY_DAYS=30

# ── Kafka ─────────────────────────────────────────────────────────────────────
SPRING_KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_BOOTSTRAP=localhost:9092

# ── Razorpay (Payment Service) ────────────────────────────────────────────────
RAZORPAY_KEY_ID=rzp_test_XXXXXXXXXXXXXXXX
RAZORPAY_KEY_SECRET=your_razorpay_key_secret
RAZORPAY_WEBHOOK_SECRET=your_razorpay_webhook_secret

# ── AWS / Media Service ───────────────────────────────────────────────────────
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=AKIAZC3VOSQUK453WDMO
AWS_SECRET_ACCESS_KEY=5rUYBIUGrkWKT2FV3BwdNBBqibMmw1UvGA0hzAqC
AWS_S3_BUCKET=safar-media-dev
AWS_CLOUDFRONT_DOMAIN=d1pei2hufbgxoj.cloudfront.net

# ── Email / Notification Service ─────────────────────────────────────────────
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=sawank.sit@gmail.com
MAIL_PASSWORD=Telnet@987

# ── Twilio (SMS OTP / Auth Service) ──────────────────────────────────────────
TWILIO_ACCOUNT_SID=your_sid
TWILIO_AUTH_TOKEN=your_token
TWILIO_PHONE_NUMBER=+1xxxxxxxxxx

# ── Elasticsearch ─────────────────────────────────────────────────────────────
SPRING_ELASTICSEARCH_URIS=http://localhost:9200

# ── CORS ─────────────────────────────────────────────────────────────────────
ALLOWED_ORIGIN=http://localhost:3000

# ── Service Host Overrides (for Docker; defaults to localhost) ────────────────
AUTH_SERVICE_HOST=localhost
USER_SERVICE_HOST=localhost
LISTING_SERVICE_HOST=localhost
SEARCH_SERVICE_HOST=localhost
BOOKING_SERVICE_HOST=localhost
PAYMENT_SERVICE_HOST=localhost
MEDIA_SERVICE_HOST=localhost
REVIEW_SERVICE_HOST=localhost
NOTIFICATION_SERVICE_HOST=localhost

# ── August Smart Lock ─────────────────────────────────────────────────────────
AUGUST_API_KEY=your_august_api_key
